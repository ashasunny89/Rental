@extends('layouts.app', ['title' => __('User Profile')])

@section('content')

@include('users.partials.header', [
    'title' => __(''),
    'description' => __(''),
    'class' => 'col-lg-12'
])   
<div class="container-fluid mt--7">
<div class="row">
<div class="col-xl-12 mb-5 mb-xl-6">
    <div class="card shadow">
        <div class="card-header border-0">
            <div class="row align-items-center">
                <div class="col">
                    <h3 class="mb-0">Rental Details</h3>
                    
                </div>
                <a class="btn bg-gradient-white  mx-3" href="{{route('rental.create')}}">
                    <i class="material-icons text-sm"></i>&nbsp;&nbsp;Add New Rent
                </a>
            </div>
        </div>
        @if(session('success'))
            <div>
                <p>{{ session('success') }}</p>
            </div>
        @endif
        <div class="table-responsive">
                        <table class="table align-items-center table-flush">
                        <thead class="thead-light">
                            <tr>
                                <th scope="col">S.No</th>
                                <th scope="col">Customer Name</th>
                                <th scope="col">Phone 1</th>
                                <th scope="col">Rental Date</th>
                                <th scope="col">Return Date</th>
                               
                            </tr>
                        </thead>
                        <tbody>

                            <tr>
                                <td>{{$rental->id }}</td>
                                <td>{{$rental->customer_name }}</td>
                                <td>{{$rental->phone1  }}</td>
                                <td>{{$rental->rental_date  }}</td>
                                <td>{{$rental->return_date }}</td>
                            </tr>
                        </tbody>
                        </table>
            </div>
    </div>
</div>
<div class="row mt-5">
        <div class="col-lg-12">
            <h3 class="mb-4">Suit Pieces Rented</h3>
            <ul>
                @foreach ($rental->suitPieces as $suitPiece)
                    <li>{{ $suitPiece->name }} ({{ $suitPiece->size }}) - {{ $suitPiece->pivot->price }}/-</li>
                @endforeach
            </ul>
        </div>
    </div>
</div>
@include('layouts.footers.auth')
</div>
<script>
    function confirmDelete(rentalid ) {
        if (confirm('Are you sure you want to delete this item?')) {
            document.getElementById('delete-form-' + rentalid ).submit();
        }
    }
</script>
@endsection